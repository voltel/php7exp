{# Одиночный комментарий #}
<article class="col-12 col-sm-6">
  <div class="card">
    <div class="card-header">
    <p>{{ comment.getPostedAtFormatted() }}</br>
    <strong>{{ comment.getUser().getEmail() }}</strong></p>
    </div>
    <div class="card-block">
      <p class="card-text">{{ comment.getComment() }}</p>
    </div>
  </div><!-- /.card -->
</article>
